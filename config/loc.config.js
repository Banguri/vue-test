

const config = {
  protocol: 'http:',
  apiHost: 'local.lglifecare.com:8080',
  baseHost: 'local.i-on.net:8000',
  dynamicSiteRouting: true,
  plugins: [],
  modules: [],
  useAPIMockup: true,
  availableThemes: [
    'unishop',
    'multikart',
    'default',
    'admin',
  ],
  defaultTheme: 'admin',
};

export default config;
